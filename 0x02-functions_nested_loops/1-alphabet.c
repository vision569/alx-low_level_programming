i#include "main.h"

/**
* main - Prints the alphabet in lowercase.
* Return: Always 0
 */
void print_alphabet_x10(void)
{
	char letter;
	for (letter = 'a'; letter <= 'z'; letter++)

		putchar(letter);
	putchar('\n');
return (0);
}
